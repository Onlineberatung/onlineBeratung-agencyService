# Logging: SLF4J (via Lombok)
logging.level.root=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=ERROR

# Keycloak
keycloak.auth-server-url=
keycloak.realm=
keycloak.bearer-only=true
keycloak.resource=agency-service
keycloak.principal-attribute=
keycloak.cors=false

# MariaDB
spring.datasource.url=jdbc:mariadb://<host>:<port>/agencyservice
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=8
spring.datasource.hikari.idle-timeout=500000
spring.datasource.hikari.maxLifetime=500000
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDB103Dialect

# Liquibase
spring.liquibase.change-log=classpath:db/changelog/agencyservice-prod-master.xml
spring.liquibase.user=
spring.liquibase.password=
